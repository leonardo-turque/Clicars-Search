## Summary

<!-- What changed and why (1–3 bullets). -->

-

## Type of change

- [ ] Feature (`feat`)
- [ ] Bug fix (`fix`)
- [ ] Documentation (`docs`)
- [ ] Chore / CI / tooling (`chore`)
- [ ] Breaking change (describe migration below)

## Test plan

- [ ] `cd backend && go test ./...`
- [ ] `cd frontend && npm test` (when frontend is touched)
- [ ] Manual check with `docker compose up -d --build` / `api.http` (when relevant)

## Checklist

- [ ] Targets `develop` (or `main` only for release/hotfix PRs)
- [ ] CHANGELOG `[Unreleased]` updated when user-facing
- [ ] No secrets or `.env` files included
